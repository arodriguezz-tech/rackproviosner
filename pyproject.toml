[project]
name = "rack-provisioner-alpha"
version = "2.2.0"
requires-python = ">=3.11"

[project.optional-dependencies]
test = ["pytest>=8", "pytest-cov>=5"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"
